{
  "name": "am-calc",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "date-input-polyfill": "^2.14.0",
    "moment": "^2.18.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-icons": "^2.2.5"
  },
  "devDependencies": {
    "node-sass": "^4.5.3",
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "all": "./server.scpt",
    "sass": "sass --watch src/styles/scss:src/styles/css",
    "deploy": "yarn build && rsync -avzP build/ yoda:mortgage/ && rm -rf build"
  }
}
